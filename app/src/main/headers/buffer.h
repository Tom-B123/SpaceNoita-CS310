#ifndef BUFFER_H
#define BUFFER_H
// Stores the memory buffer
#include "app.h"
#include "engine.h"

// char* buf;// = init_buf(WORLD_WIDTH,WORLD_HEIGHT);
// char* output_buf;// = init_output_buf(WORLD_WIDTH,WORLD_HEIGHT);
#endif
